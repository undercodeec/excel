services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: sqlite:////data/planificacion.db
      SECRET_KEY: ${SECRET_KEY:-dev-secret-change-me}
    volumes:
      - app_data:/data
    restart: unless-stopped

volumes:
  app_data:
